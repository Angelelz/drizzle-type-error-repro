{
  "dependencies": {
    "@test/database": "workspace:*",
    "next": "14.0.4",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@testrepo/tsconfig": "workspace:*",
    "@types/node": "18.17.1",
    "@types/react": "18.2.45",
    "@types/react-dom": "18.2.15",
    "eslint-config-testrepo": "workspace:*"
  },
  "name": "@test/client",
  "private": true,
  "scripts": {
    "build": "vercel build",
    "format": "pnpm run lint --fix",
    "lint": "eslint --max-warnings 0 --ext .js,.ts,.jsx,.tsx . --cache --cache-strategy content",
    "pull-vercel": "npx vercel pull --yes",
    "start": "vercel dev",
    "test": "jest",
    "tsc": "tsc"
  }
}
